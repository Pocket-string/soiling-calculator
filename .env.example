# ============================================
# Soiling Calculator — Environment Variables
# ============================================
# Copia este archivo a .env.local y rellena los valores.
# cp .env.example .env.local

# --- Supabase (requeridas) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- Supabase (server-side, requerida para service client) ---
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- App ---
NEXT_PUBLIC_SITE_URL=http://localhost:3000
ADMIN_EMAIL=admin@example.com

# --- Google Maps (opcional, para LocationPicker) ---
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# --- Email (opcional, notificaciones de leads) ---
RESEND_API_KEY=your-resend-api-key
RESEND_FROM_EMAIL=App Name <noreply@yourdomain.com>

# --- Integraciones de inversores (opcional) ---
# Clave de 32 bytes en hex (64 chars) para encriptar credenciales de inversores
# Generar con: openssl rand -hex 32
INTEGRATION_ENCRYPTION_KEY=

# --- Cron (opcional, para auto-sync de inversores) ---
# Token secreto de al menos 32 chars para proteger el endpoint cron
# Generar con: openssl rand -hex 32
CRON_SECRET=

# --- Dokploy (solo para deploy scripts, NO usada por la app) ---
# DOKPLOY_API_KEY=your-dokploy-api-key
# DOKPLOY_URL=https://your-dokploy-instance.com/api
# DOKPLOY_APP_ID=your-app-id

# --- Supabase Management API (solo para seed/migration scripts) ---
# SUPABASE_ACCESS_TOKEN=your-management-api-token
# SUPABASE_PROJECT_REF=your-project-ref
